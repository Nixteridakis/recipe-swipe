/**
 * Default Recipe Book API origin (no trailing slash).
 * Mode: production zip (runs after next build)
 * Source: NEXT_PUBLIC_APP_URL or Netlify URL/DEPLOY_PRIME_URL (pnpm run build regenerates this zip + api-base.js)
 */
var RECIPE_BOOK_API_BASE = "test";
